import { colors as colors_ } from "src/helpers/styleColors";
import { IColor, IMedia } from "src/helpers/interfaces";
import {
  media as media_,
  mediaRevers as mediaRevers_,
} from "src/helpers/styleBreakpoints";

export const colors: IColor = colors_;
export const media: IMedia = media_;
export const mediaRevers: IMedia = mediaRevers_;
