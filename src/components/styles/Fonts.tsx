import { createGlobalStyle } from "styled-components";

const Fonts = createGlobalStyle`
  :root {
    --font-brand: "Fira Sans", sans-serif, Arial;
    --font-gemsbuck: "Gemsbuck", sans-serif, Arial;
  }
`;

export { Fonts };
